{-# OPTIONS --without-K --exact-split #-}

-- Re-export shim: PaperSemantics = PaperOrder (<=, Comp, Coherent) + PaperTyping (FinMem).
module MIN.Domain.Kernel where

open import MIN.Domain.Membership public
